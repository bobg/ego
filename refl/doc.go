/*

Package refl is a wrapper around the standard reflect package.

It adds support for interface types, allows adding to a type's method
set, and prefers returning errors to panicking.

*/
package refl
